import type { Metadata } from "next";
export const metadata: Metadata = { title: "Transition CDI → Freelance", description: "Combien facturer en freelance pour gagner autant qu'en CDI ? Compare salaire net, avantages et charges." };
export default function L({ children }: { children: React.ReactNode }) { return children; }
