import type { Metadata } from "next";
export const metadata: Metadata = { title: "Calendrier fiscal freelance", description: "Toutes tes échéances fiscales sur 12 mois : URSSAF, TVA, IS, IR avec montants estimés à provisionner." };
export default function L({ children }: { children: React.ReactNode }) { return children; }
