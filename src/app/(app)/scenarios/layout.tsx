import type { Metadata } from "next";
export const metadata: Metadata = { title: "Scénarios", description: "Compare différents scénarios financiers : perte de client, hausse de TJM, changement de statut." };
export default function L({ children }: { children: React.ReactNode }) { return children; }
