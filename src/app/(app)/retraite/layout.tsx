import type { Metadata } from "next";
export const metadata: Metadata = { title: "Retraite Freelance vs CDI" };
export default function L({ children }: { children: React.ReactNode }) { return children; }
