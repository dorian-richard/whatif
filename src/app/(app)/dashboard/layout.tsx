import type { Metadata } from "next";
export const metadata: Metadata = { title: "Tableau de bord", description: "Vue d'ensemble de ton activité freelance : CA, net, météo financière et indicateurs clés." };
export default function L({ children }: { children: React.ReactNode }) { return children; }
