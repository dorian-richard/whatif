import type { Metadata } from "next";
export const metadata: Metadata = { title: "Simulateur de revenus freelance", description: "Simule ton CA et ton net mois par mois en fonction de tes clients, ton TJM, tes vacances et ton statut juridique." };
export default function L({ children }: { children: React.ReactNode }) { return children; }
